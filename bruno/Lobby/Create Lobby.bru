meta {
  name: Create Lobby
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/lobby
  body: json
  auth: inherit
}

body:json {
  {
    "playerId": "testPlayerIdOne"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
